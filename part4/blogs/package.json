{
	"name": "part4",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"start": "cross-env NODE_ENV=production node ./index.js",
		"dev": "cross-env NODE_ENV=development nodemon --env-file=.env ./index.js",
		"test": "cross-env NODE_ENV=test node --test --test-concurrency=1 --env-file=.env",
		"start:test": "cross-env NODE_ENV=test node --env-file=.env ./index.js"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"cross-env": "^7.0.3",
		"nodemon": "^3.1.0",
		"supertest": "^6.3.4"
	},
	"dependencies": {
		"bcrypt": "^5.1.1",
		"cors": "^2.8.5",
		"express": "^4.18.3",
		"express-async-errors": "^3.1.1",
		"jsonwebtoken": "^9.0.2",
		"mongoose": "^8.2.1",
		"morgan": "^1.10.0"
	}
}

